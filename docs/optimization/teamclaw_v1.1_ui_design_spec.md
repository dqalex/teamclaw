# TeamClaw v1.1 UI 设计规范（完整版）

> **版本**：v1.1 UI 设计稿（完整版）  
> **日期**：2026-03-26  
> **目的**：设计师稿输出规范，供 UI 设计参考  
> **前置文档**：[LLM 执行规划](teamclaw_v1.1_llm_execution_plan.md) / [UI 重构规划](teamclaw_v1.1-ui-redesign.md)

---

## 一、设计愿景

### 1.1 五大设计原则

```
┌─────────────────────────────────────────────────────────────┐
│                  TeamClaw v1.1 Design Principles           │
│                                                              │
│  1. 清晰的信息架构（Clear Information Architecture）          │
│     └── 符合 Mental Model 的导航分组                         │
│                                                              │
│  2. 一致的交互模式（Consistent Interaction Patterns）         │
│     └── 统一的状态反馈、过渡动画、错误处理                    │
│                                                              │
│  3. 渐进式复杂度（Progressive Complexity）                   │
│     └── 基础视图 → 高级视图 → 专业视图                        │
│                                                              │
│  4. 移动优先响应式（Mobile-First Responsive）               │
│     └── 核心功能移动端可用                                    │
│                                                              │
│  5. 可访问性优先（Accessibility First）                      │
│     └── 键盘导航、屏幕阅读器、色彩对比度                       │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 视觉方向

```
主题：Deep Command Center（深空指挥中心）
├── 深色系为主：#050810 → #101420
├── 强调色：AI 电光青 #06b6d4
├── 人类色：协作紫 #8b5cf6
├── 信息密度：中高（专业用户）
└── 交互反馈：微光晕 + 流畅动画
```

### 1.3 核心视觉升级（v1.0 → v1.1）

| 维度 | v1.0 | v1.1 |
|------|------|------|
| **图标** | Lucide React 混用 | 统一 Icon System（Lucide + 自定义） |
| **间距** | 随意（px, py, gap-3混用） | 8px 网格系统 |
| **圆角** | rounded-3xl 为主 | 统一半径系统（sm/md/lg/xl） |
| **阴影** | 分散定义 | 统一 Elevation Tokens |
| **动画** | 分散定义 | Motion Primitives |
| **状态** | CSS 类名 | Design Tokens 变量 |

---

## 二、色彩系统

### 2.1 主色板

```css
/* 品牌色 Brand */
--color-brand:           #6366f1;    /* 主品牌色 Indigo */
--color-brand-light:     #818cf8;    /* 品牌浅色 */
--color-brand-dark:      #4f46e5;    /* 品牌深色 */

/* AI 色 */
--color-ai:              #06b6d4;    /* 电光青 - AI 相关高亮 */
--color-ai-light:        #22d3ee;    /* AI 浅色 */
--color-ai-glow:        rgba(6, 182, 212, 0.3);  /* AI 光晕 */

/* 人类协作色 */
--color-human:            #8b5cf6;    /* 协作紫 */
--color-human-light:      #a78bfa;    /* 协作浅色 */

/* 语义色 */
--color-success:         #10b981;    /* 成功绿 */
--color-warning:         #f59e0b;    /* 警告橙 */
--color-danger:          #ef4444;    /* 危险红 */
--color-info:            #3b82f6;    /* 信息蓝 */

/* 中性色 */
--color-bg-primary:      #050810;    /* 主背景 */
--color-bg-secondary:    #0a0f1a;    /* 次级背景 */
--color-bg-elevated:     #101420;    /* 卡片/浮层 */
--color-bg-hover:        #1a2235;    /* 悬停态 */
--color-border:          #1e293b;    /* 边框 */
--color-border-strong:   #334155;    /* 强调边框 */
--color-text-primary:    #f1f5f9;    /* 主文字 */
--color-text-secondary:  #94a3b8;    /* 次级文字 */
--color-text-muted:      #64748b;    /* 弱化文字 */
```

### 2.2 渐变与光效

```css
/* 背景渐变 */
--gradient-hero:         linear-gradient(135deg, #050810 0%, #0a0f1a 50%, #0f172a 100%);
--gradient-card:         linear-gradient(180deg, rgba(99, 102, 241, 0.05) 0%, transparent 100%);

/* AI 相关光效 */
--glow-ai:               0 0 20px rgba(6, 182, 212, 0.3);
--glow-brand:            0 0 20px rgba(99, 102, 241, 0.3);

/* 边框光效 */
--border-glow-ai:        0 0 0 1px rgba(6, 182, 212, 0.5);
```

---

## 三、字体系统

### 3.1 字体族

```css
/* 主字体 */
--font-sans:    'Plus Jakarta Sans', system-ui, -apple-system, sans-serif;

/* 代码字体 */
--font-mono:    'JetBrains Mono', 'Fira Code', Consolas, monospace;

/* 数字字体 */
--font-number:  'Tabular Nums', 'Plus Jakarta Sans', sans-serif;
```

### 3.2 字体规格

| 元素 | 字号 | 字重 | 行高 | 字间距 |
|------|------|------|------|--------|
| **H1 页面标题** | 28px | 700 | 1.2 | -0.02em |
| **H2 区块标题** | 22px | 600 | 1.3 | -0.01em |
| **H3 卡片标题** | 16px | 600 | 1.4 | 0 |
| **Body 正文** | 14px | 400 | 1.5 | 0 |
| **Caption 说明** | 12px | 400 | 1.4 | 0.01em |
| **Code 代码** | 13px | 400 | 1.5 | 0 |
| **Label 标签** | 11px | 500 | 1.2 | 0.05em |

---

## 四、间距系统

### 4.1 基础间距（8px 网格）

```css
--space-0:     0;
--space-1:     4px;    /* 1 unit */
--space-2:     8px;    /* 2 units */
--space-3:     12px;   /* 3 units */
--space-4:     16px;   /* 4 units */
--space-5:     20px;   /* 5 units */
--space-6:     24px;   /* 6 units */
--space-8:     32px;   /* 8 units */
--space-10:    40px;   /* 10 units */
--space-12:    48px;   /* 12 units */
--space-16:    64px;   /* 16 units */
```

---

## 五、圆角系统

```css
--radius-sm:     4px;     /* 小圆角 - 标签/徽章 */
--radius-md:     8px;     /* 中圆角 - 按钮/输入框 */
--radius-lg:     12px;    /* 大圆角 - 卡片 */
--radius-xl:     16px;    /* 特大圆角 - 模态框 */
--radius-2xl:    24px;   /* 超大圆角 - 大型容器 */
--radius-full:   9999px; /* 全圆角 - 头像/图标按钮 */
```

---

## 六、阴影系统

```css
--shadow-xs:     0 1px 2px rgba(0, 0, 0, 0.2);
--shadow-sm:     0 1px 3px rgba(0, 0, 0, 0.25), 0 1px 2px rgba(0, 0, 0, 0.2);
--shadow-md:     0 4px 6px rgba(0, 0, 0, 0.25), 0 2px 4px rgba(0, 0, 0, 0.2);
--shadow-lg:     0 10px 15px rgba(0, 0, 0, 0.25), 0 4px 6px rgba(0, 0, 0, 0.2);
--shadow-xl:     0 20px 25px rgba(0, 0, 0, 0.25), 0 10px 10px rgba(0, 0, 0, 0.2);
--shadow-glow:   0 0 20px rgba(6, 182, 212, 0.3);
```

---

## 七、动效规范

### 7.1 动画时长

```css
--duration-fast:     150ms;    /* 快速反馈 - 按钮点击/切换 */
--duration-normal:   300ms;    /* 正常过渡 - 页面切换/展开收起 */
--duration-slow:     500ms;    /* 缓慢动画 - 数据加载/进度 */
--duration-bounce:   600ms;    /* 弹性效果 */
```

### 7.2 缓动函数

```css
--ease-out:          cubic-bezier(0.16, 1, 0.3, 1);   /* 标准退出 */
--ease-in:           cubic-bezier(0.7, 0, 0.84, 0);   /* 标准进入 */
--ease-in-out:       cubic-bezier(0.65, 0, 0.35, 1); /* 对称过渡 */
--ease-bounce:       cubic-bezier(0.68, -0.55, 0.265, 1.55); /* 弹性 */
--ease-spring:       cubic-bezier(0.175, 0.885, 0.32, 1.275); /* 弹簧 */
```

### 7.3 动画使用场景

| 场景 | 动画类型 | 时长 | 缓动 |
|------|---------|------|------|
| 按钮点击 | scale 0.95 → 1 | 150ms | ease-out |
| 卡片悬停 | shadow + translateY(-2px) | 200ms | ease-out |
| 页面进入 | fadeIn + translateY(8px → 0) | 300ms | ease-spring |
| 下拉展开 | height + opacity | 250ms | ease-out |
| Toast 出现 | translateY(-100%) → 0 + fadeIn | 300ms | ease-spring |
| Toast 消失 | fadeOut + translateY(0 → -100%) | 200ms | ease-in |
| 加载骨架 | shimmer 动画 | 1500ms | linear (repeat) |
| 进度条 | width 变化 | 300ms | ease-out |
| 侧边栏折叠 | width 变化 | 250ms | ease-out |

---

## 八、布局系统

### 8.1 AppShell 2.0 布局结构

```
┌────────────────────────────────────────────────────────────────────────────┐
│                              Header (56px)                                 │
│  ┌────────┬──────────────────────────────────────────────────────────┐  │
│  │        │                                                          │  │
│  │        │                                                          │  │
│  │        │                                                          │  │
│  │Sidebar │                    Main Content                           │  │
│  │(240px) │                    (flex: 1)                            │  │
│  │        │                                                          │  │
│  │        │                                                          │  │
│  │        │                                                          │  │
│  │        │                                                          │  │
│  └────────┴──────────────────────────────────────────────────────────┘  │
│                                                                            │
│                         Command Bar (⌘K Overlay)                           │
└────────────────────────────────────────────────────────────────────────────┘
```

**Header 规格**：
- 高度：56px
- 背景：半透明 + 毛玻璃效果 `backdrop-filter: blur(12px)`
- 边框底部：`1px solid var(--color-border)`
- 内容：Logo (32px) | 面包屑 | 搜索框 | 通知 | 用户头像

**Header Props 接口**：

```typescript
interface Header2Props {
  title?: string;
  breadcrumb?: BreadcrumbItem[];
  actions?: React.ReactNode;
  showBack?: boolean;
  onBack?: () => void;
  variant?: 'default' | 'transparent' | 'blur';
}
```

**Sidebar 规格**：
- 宽度：240px（展开）/ 64px（折叠）
- 背景：`var(--color-bg-secondary)`
- 边框右侧：`1px solid var(--color-border)`
- 可折叠动画：250ms ease-out

**Sidebar Props 接口**：

```typescript
interface Sidebar2Props {
  items: NavItem[];
  groups: NavGroup[];
  activeId: string;
  collapsed: boolean;
  onToggle: () => void;
  onNavigate: (id: string) => void;
}

interface NavGroup {
  id: string;
  label: string;
  icon?: React.ElementType;
  items: NavItem[];
}

interface NavItem {
  id: string;
  label: string;
  icon: React.ElementType;
  href: string;
  badge?: number;
  children?: NavItem[];
}
```

### 8.2 侧边栏导航结构

```
┌────────────────────────────────────┐
│  Logo + 折叠按钮                   │
├────────────────────────────────────┤
│                                    │
│  ┌─ 快捷入口 ──────────────────┐  │
│  │ 🏠 Dashboard                │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌─ 工作区 ────────────────────┐  │
│  │ 📁 项目                     │  │
│  │  └ 📋 任务                  │  │
│  │  └ 📄 文档                  │  │
│  │  └ 👥 成员                  │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌─ 自动化 ────────────────────┐  │
│  │ ⏰ 定时                     │  │
│  │  └ 🔀 流程                  │  │
│  │  └ ⚡ 触发                  │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌─ 资产库 ────────────────────┐  │
│  │ 🛠️ 技能                    │  │
│  │  └ 🏪 市场                  │  │
│  │  └ 📋 SOP                   │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌─ 协作 ──────────────────────┐  │
│  │ 📦 交付                    │  │
│  │  └ ✅ 审批                  │  │
│  │  └ 💬 记录                  │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌─ 运营 ──────────────────────┐  │
│  │ 🛒 市场                     │  │
│  │  └ 📑 订阅                  │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌─ 管理 ──────────────────────┐  │
│  │ ⚙️ 设置                    │  │
│  │  └ 🤖 OpenClaw             │  │
│  │  └ 🔌 插件                  │  │
│  │  └ 📡 渠道                  │  │
│  └──────────────────────────────┘  │
│                                    │
├────────────────────────────────────┤
│  ⚡ OpenClaw 状态指示器             │
│  ● 运行中 | 5 Sessions             │
└────────────────────────────────────┘
```

---

## 九、Command Bar 设计

### 9.1 Command Bar 界面

```
┌─────────────────────────────────────────────┐
│  ⌘K  Command Bar                           │
├─────────────────────────────────────────────┤
│  🔍 搜索功能、页面、设置...                   │
├─────────────────────────────────────────────┤
│  RECENT                                    │
│  ├── 创建新任务                             │
│  ├── 查看项目进度                           │
│  └── OpenClaw 设置                         │
├─────────────────────────────────────────────┤
│  ACTIONS                                   │
│  ├── 🌙 切换主题                            │
│  ├── 🚪 登出                               │
│  └── ⚙️ 系统设置                           │
└─────────────────────────────────────────────┘
```

### 9.2 Command Bar Props

```typescript
interface CommandBarProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

// 快捷命令示例
const commands = [
  { id: 'goto-dashboard', label: '前往仪表盘', shortcut: 'G D' },
  { id: 'goto-tasks', label: '前往任务', shortcut: 'G T' },
  { id: 'create-task', label: '创建任务', shortcut: 'C T' },
  { id: 'toggle-theme', label: '切换主题', shortcut: '⌘ J' },
  { id: 'open-settings', label: '打开设置', shortcut: '⌘ ,' },
];
```

---

## 十、组件系统

### 10.1 组件分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Component Layers                          │
│                                                              │
│  Layer 1: Primitives（原子组件）                             │
│  ├── Button, Input, Select, Checkbox                         │
│  ├── Badge, Tag, Avatar                                      │
│  ├── Icon, Text, Heading                                     │
│  └── Divider, Spacer                                         │
│                                                              │
│  Layer 2: Composites（复合组件）                             │
│  ├── Card, Panel, Modal, Drawer                             │
│  ├── Table, List, Tree                                       │
│  ├── Form, Field, FieldGroup                                 │
│  └── Tabs, Accordion, Stepper                                │
│                                                              │
│  Layer 3: Patterns（业务模式）                                │
│  ├── StatCard, QuickLink, StatusIndicator                   │
│  ├── DataTable, FilterBar, Pagination                       │
│  ├── SearchBar, CommandPalette                              │
│  └── Notification, Toast, Confirmation                       │
│                                                              │
│  Layer 4: Features（功能模块）                                │
│  ├── TaskCard, TaskBoard                                    │
│  ├── ChatBubble, ChatPanel                                  │
│  ├── SkillCard, WorkflowNode                               │
│  └── MarketplaceItem, PricingCard                           │
└─────────────────────────────────────────────────────────────┘
```

### 10.2 Button v2

```typescript
interface ButtonProps {
  variant?: 'primary' | 'secondary' | 'ghost' | 'danger' | 'ai';
  size?: 'xs' | 'sm' | 'md' | 'lg';
  loading?: boolean;
  leftIcon?: React.ReactNode;
  rightIcon?: React.ReactNode;
  fullWidth?: boolean;
}
```

**视觉规范**：

| 变体 | 默认 | 悬停 | 按下 | 禁用 |
|------|------|------|------|------|
| **primary** | bg: brand | bg: brand-dark | scale: 0.98 | opacity: 0.5 |
| **secondary** | bg: bg-elevated, border | bg: bg-hover | scale: 0.98 | opacity: 0.5 |
| **ghost** | bg: transparent | bg: bg-hover | scale: 0.98 | opacity: 0.5 |
| **danger** | bg: danger | bg: danger-dark | scale: 0.98 | opacity: 0.5 |
| **ai** | bg: ai, glow | bg: ai-light | scale: 0.98 | opacity: 0.5 |

### 10.3 Card v2

```typescript
interface CardProps {
  variant?: 'default' | 'bordered' | 'elevated' | 'ghost';
  padding?: 'none' | 'sm' | 'md' | 'lg';
  interactive?: boolean;
}
```

### 10.4 DataTable v2

```typescript
interface DataTableProps<T> {
  columns: Column<T>[];
  data: T[];
  selectable?: boolean;
  sortable?: boolean;
  filterable?: boolean;
  pagination?: PaginationConfig;
  onRowClick?: (row: T) => void;
  emptyState?: React.ReactNode;
  loading?: boolean;
}
```

**视觉规范**：

| 元素 | 样式 |
|------|------|
| 表头 | text-secondary, font-weight: 600, border-bottom: 1px border |
| 行 | hover: bg-hover, cursor: pointer |
| 选中行 | bg: brand/10, border-left: 2px brand |
| 排序图标 | 12px, text-muted |
| 分页 | 居右, 间距 16px |

### 10.5 新增组件清单

| 组件 | 用途 | 优先级 |
|------|------|--------|
| **CommandBar** | 全局命令面板（⌘K） | 🔴 P0 |
| **Breadcrumb** | 面包屑导航 | 🔴 P0 |
| **Notification** | 通知中心 | 🔴 P0 |
| **DataTable** | 数据表格 | 🔴 P0 |
| **KanbanBoard** | 看板组件（从 TaskBoard 抽象） | 🟡 P1 |
| **WorkflowCanvas** | 可视化工作流编辑器 | 🟡 P1 |
| **SkillGraph** | 技能依赖可视化 | 🟢 P2 |
| **AuditLogViewer** | 审计日志查看器 | 🟢 P2 |

---

## 十一、信息架构

### 11.1 新导航结构

```
┌─ 快捷入口 ─────────────────────────────────────────────────────┐
│  Dashboard ← 首页仪表盘（Gateway + 核心指标 + 快捷入口）        │
└────────────────────────────────────────────────────────────────┘

┌─ 工作区 ─────────────────────────────────────────────────────┐
│   项目 ← projects/     项目列表 + 里程碑                       │
│    │                      ↓                                     │
│    ├── 任务 ← tasks/    看板/列表视图                          │
│    ├── 文档 ← wiki/     知识库 + 编辑器                       │
│    └── 成员 ← members/  团队成员（人 + AI）                   │
└────────────────────────────────────────────────────────────────┘

┌─ 自动化 ─────────────────────────────────────────────────────┐
│   定时 ← schedule/     Cron 任务管理                           │
│    │                      ↓                                     │
│    ├── 流程 ← workflows/ Workflow 可视化编辑器 🆕            │
│    └── 触发 ← triggers/   事件触发规则 🆕                     │
└────────────────────────────────────────────────────────────────┘

┌─ 资产库 ─────────────────────────────────────────────────────┐
│   技能 ← skills/       技能管理（自进化）                      │
│    │                      ↓                                     │
│    ├── 市场 ← skillhub/  ClawHub 技能市场 🆕                   │
│    └── SOP  ← sop/       SOP 模板库                           │
└────────────────────────────────────────────────────────────────┘

┌─ 协作 ───────────────────────────────────────────────────────┐
│   交付 ← deliveries/   交付物审核                             │
│    │                      ↓                                     │
│    ├── 审批 ← approvals/ 审批流程                            │
│    ├── 记录 ← sessions/   对话记录                            │
│    └── OKR  ← okr/        目标管理 🆕                          │
└────────────────────────────────────────────────────────────────┘

┌─ 运营 ───────────────────────────────────────────────────────┐
│   市场 ← marketplace/   AI 服务市场（Consumer）               │
│    │                      ↓                                     │
│    ├── 订阅 ← subscriptions/ 已订阅服务 🆕                    │
│    └── 收益 ← earnings/    收益中心 🆕                        │
└────────────────────────────────────────────────────────────────┘

┌─ 管理 ───────────────────────────────────────────────────────┐
│   设置 ← settings/      系统设置（Tab 分类）                  │
│    │                      ↓                                     │
│    ├── 通用 ← /general   主题/语言/数据                       │
│    ├── OpenClaw ← /openclaw Gateway + Workspace              │
│    ├── 插件 ← /plugins   插件管理 🆕                          │
│    ├── 渠道 ← /channels  渠道管理 🆕                          │
│    ├── 安全 ← /security  安全设置                             │
│    ├── 首页 ← /landing   首页内容管理                          │
│    ├── 调试 ← /debug     调试工具                              │
│    └── 关于 ← /about      关于系统                              │
└────────────────────────────────────────────────────────────────┘
```

### 11.2 页面重组对照表

| v1.0 页面 | v1.1 归属 | 变更说明 |
|----------|----------|---------|
| dashboard/ | 快捷入口 | 保持独立，作为首页 |
| tasks/ | 工作区 | 保持独立 |
| wiki/ | 工作区 | 保持独立 |
| projects/ | 工作区 | 保持独立 |
| members/ | 工作区 | 保持独立 |
| schedule/ | 自动化 | 保持独立 |
| scheduler/ | 自动化 | **合并到 schedule/** |
| skills/ | 资产库 | 保持独立 |
| skillhub/ | 资产库/市场 | 重构为 ClawHub 市场 |
| sop/ | 资产库 | 保持独立 |
| deliveries/ | 协作 | 保持独立 |
| approvals/ | 协作 | 保持独立 |
| sessions/ | 协作 | 保持独立 |
| agents/ | Agent | 保持独立 |
| users/ | 管理 | **移除（仅管理员可见）** |
| settings/ | 管理/设置 | **拆分为子路由** |
| marketplace/ | 运营 | 保持独立 |
| blog/ | 其他 | 保持独立 |

---

## 十二、页面设计规范

### 12.1 Dashboard 2.0（指挥中心）

**设计目标**：从信息聚合 → 指挥中心，提供清晰的系统状态概览

```
┌────────────────────────────────────────────────────────────────────────────┐
│  Dashboard                                                                   │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌─ Gateway 状态卡片 (醒目) ──────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐          │   │
│  │  │ 🟢 运行中 │  │  99.9%   │  │   12h    │  │   5/6    │          │   │
│  │  │  Status  │  │ Uptime   │  │ Sessions │  │ Channels │          │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘          │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│  ┌─ 今日概览 ───────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐   │   │
│  │  │   8    │  │   3    │  │   2    │  │   2    │  │ 1,234  │   │   │
│  │  │进行中   │  │ 待审核  │  │ 待交付  │  │ 工作 AI │  │ Tokens │   │   │
│  │  │ 任务    │  │ 交付    │  │ 交付    │  │        │  │        │   │   │
│  │  └────────┘  └────────┘  └────────┘  └────────┘  └────────┘   │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│  ┌─ 快捷入口 ───────────────────────────────────────────────────────┐   │
│  │  [+ 新建任务]  [📋 查看任务]  [📄 新建文档]  [⏰ 定时任务]      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│  ┌─ AI 团队状态 ──────┐  ┌─ 项目进度 ──────────────────────────────────┐ │
│  │                    │  │                                              │ │
│  │ 🤖 AI-Copywriter   │  │  ████████████░░░░░░░░  品牌文案  65%      │ │
│  │    运行中 ●        │  │  ████████░░░░░░░░░░░░░  竞品分析  40%      │ │
│  │                    │  │  ████████████████████  用户手册  95%        │ │
│  │ 🤖 AI-Researcher   │  │                                              │ │
│  │    空闲 ○         │  └──────────────────────────────────────────────┘ │
│  │                    │                                                │
│  │ 🤖 AI-Designer     │  ┌─ 最近交付 ──────────────────────────────────┐ │
│  │    空闲 ○         │  │                                              │ │
│  │                    │  │  📄 品牌文案 v2.1  →  待审核                 │ │
│  └────────────────────┘  │  📄 封面设计稿    →  已通过                 │ │
│                            └──────────────────────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

**设计要点**：
- Gateway 状态卡片使用 AI 青色高亮，醒目展示系统核心状态
- 数字统计卡片使用 Tabular Nums 字体，保证对齐
- AI 团队状态使用机器人图标 + 状态点（运行中绿色/空闲灰色）
- 项目进度使用细长进度条，减少视觉重量

---

### 12.2 Settings 2.0（路由化设置）

**设计目标**：从单页 Tab → 分栏导航，提升设置的可维护性

```
┌────────────────────────────────────────────────────────────────────────────┐
│  Settings 2.0 — 路由化设置                                                  │
│                                                                            │
│  设置分组（Sidebar）            当前设置内容                                  │
│  ┌────────────────────┐  ┌────────────────────────────────────┐   │
│  │ 🔧 通用             │  │                                    │   │
│  │ ├── 外观           │  │  外观设置                          │   │
│  │ ├── 语言           │  │                                    │   │
│  │ └── 数据           │  │  主题: [浅色] [深色] [跟随系统]     │   │
│  │                    │  │                                    │   │
│  │ 🤖 OpenClaw        │  │  语言: [中文] [English]            │   │
│  │ ├── Gateway        │  │                                    │   │
│  │ ├── Workspace      │  │  ────────────────────────────────  │   │
│  │ └── Agent          │  │                                    │   │
│  │                    │  │  数据管理                          │   │
│  │ 🔌 插件 🆕        │  │  数据库: SQLite                    │   │
│  │ ├── 已安装        │  │  位置: ~/.teamclaw/data            │   │
│  │ ├── 市场 🆕      │  │                                    │   │
│  │ └── 配置 🆕      │  │  [导出数据] [刷新数据] [备份]       │   │
│  │                    │  │                                    │   │
│  │ 📡 渠道 🆕        │  └────────────────────────────────────┘   │
│  │ ├── WhatsApp       │                                            │
│  │ ├── Telegram       │                                            │
│  │ └── Discord        │                                            │
│  │                    │                                            │
│  │ 🔒 安全            │                                            │
│  │ ├── 密码           │                                            │
│  │ ├── 安全码         │                                            │
│  │ └── SSRF           │                                            │
│  │                    │                                            │
│  │ 📝 内容            │                                            │
│  │ └── 首页           │                                            │
│  │                    │                                            │
│  │ 🔧 工具            │                                            │
│  │ ├── 调试           │                                            │
│  │ └── 重置           │                                            │
│  │                    │                                            │
│  │ ℹ️ 关于            │                                            │
│  └────────────────────┘                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

**设计要点**：
- 左侧导航固定宽度 200px，可折叠
- 当前选中项使用品牌色背景高亮
- 右侧内容区最大宽度 720px，居中展示
- 每个设置区块使用卡片包裹，间距 24px

---

### 12.3 Workflow Editor（可视化编辑器）

**设计目标**：提供直观的 DAG 可视化编辑体验

```
┌────────────────────────────────────────────────────────────────────────────┐
│  Workflow Editor 2.0 — 可视化工作流                                          │
│                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ [← 返回]  [Workflow Name]              [保存] [运行] [发布]         │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│  ┌──────────────┬──────────────────────────────────────────────────────┐  │
│  │  节点面板    │               Canvas                                   │  │
│  │              │                                                      │  │
│  │  ┌────────┐ │  ┌─────────────────────────────────────────────┐     │  │
│  │  │ Trigger│ │  │                                             │     │  │
│  │  │ ○ Cron │ │  │     ┌──────┐     ┌──────┐                   │     │  │
│  │  │ ○ Event│ │  │     │ Start│────▶│ Step1│                   │     │  │
│  │  │ ○ Webhook│ │  │     └──────┘     └──────┘                   │     │  │
│  │  └────────┘ │  │                    │                           │     │  │
│  │  ┌────────┐ │  │            ┌──────▼──────┐                     │     │  │
│  │  │ Action │ │  │            │  Condition  │                     │     │  │
│  │  │ ○ Task │ │  │            └──────┬──────┘                     │     │  │
│  │  │ ○ Notify│ │  │         ┌───────┴───────┐                    │     │  │
│  │  │ ○ Webhook│ │  │         ▼               ▼                    │     │  │
│  │  │ ○ Delay │ │  │    ┌──────┐        ┌──────┐                  │     │  │
│  │  └────────┘ │  │    │ Step2│        │ Step3│                  │     │  │
│  │  ┌────────┐ │  │    └──────┘        └──────┘                  │     │  │
│  │  │ Logic  │ │  │                                             │     │  │
│  │  │ ○ If   │ │  └─────────────────────────────────────────────┘     │  │
│  │  │ ○ Loop │ │                                                      │  │
│  │  │ ○ Merge│ │  ┌──────────────────────────────────────────────┐    │  │
│  │  └────────┘ │  │  属性面板                                     │    │  │
│  │  ┌────────┐ │  │  Node: Cron Trigger                          │    │  │
│  │  │ AI     │ │  │  ──────────────────────────────              │    │  │
│  │  │ ○ Ask  │ │  │  表达式: 0 0 9 * * *                        │    │  │
│  │  │ ○ Generate│ │ │  时区: Asia/Shanghai                       │    │  │
│  │  │ ○ Summarize│ ││  ──────────────────────────────              │    │  │
│  │  └────────┘ │  └──────────────────────────────────────────────┘    │  │
│  └──────────────┴──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────┘
```

**节点视觉规范**：

| 节点类型 | 边框色 | 图标 | 特殊效果 |
|---------|--------|------|---------|
| Trigger | `#8b5cf6` (紫) | ⚡ | 左侧脉冲动画 |
| SOP | `#6366f1` (品牌) | 📋 | - |
| AI | `#06b6d4` (青) | 🤖 | 运行时发光 |
| 人工 | `#f59e0b` (橙) | 👤 | - |
| 条件 | `#10b981` (绿) | 🔀 | - |
| 循环 | `#ec4899` (粉) | 🔄 | - |
| 并行 | `#3b82f6` (蓝) | ⚡ | - |
| 渲染 | `#8b5cf6` (紫) | 🎨 | - |
| 审核 | `#f59e0b` (橙) | ✅ | - |
| 结束 | `#64748b` (灰) | ⏹ | - |

**边/连线规范**：
- 默认边：`#334155` 实线，2px
- 选中边：`#6366f1` 实线，2px
- 条件边：虚线 `stroke-dasharray: 5 5`
- 边的箭头：`polygon` 填充

---

### 12.4 Marketplace（AI 服务市场）

**设计目标**：清晰的商品展示，引导消费者发现和订阅服务

```
┌────────────────────────────────────────────────────────────────────────────┐
│  AI 服务市场                                                                 │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌─ 搜索与筛选 ───────────────────────────────────────────────────────┐   │
│  │  🔍 搜索服务...                        [全部] [内容生成] [分析] [助手]│   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│  ┌─ 服务卡片 ─────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐     │   │
│  │  │    [图标]      │  │    [图标]      │  │    [图标]      │     │   │
│  │  │   品牌文案助手  │  │   竞品分析器   │  │   小红书写作   │     │   │
│  │  │                │  │                │  │                │     │   │
│  │  │ ⭐⭐⭐⭐⭐ 4.9  │  │ ⭐⭐⭐⭐ 4.7  │  │ ⭐⭐⭐⭐⭐ 4.8  │     │   │
│  │  │ (1,234)       │  │ (856)         │  │ (2,301)       │     │   │
│  │  │                │  │                │  │                │     │   │
│  │  │ 🔥 热门       │  │ 🆕 新上架     │  │                │     │   │
│  │  │                │  │                │  │                │     │   │
│  │  │ 💰 100 Credits │  │ 💰 50 Credits │  │ 💰 200 Credits│     │   │
│  │  │ [查看详情]     │  │ [查看详情]     │  │ [查看详情]     │     │   │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘     │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

**服务卡片规范**：
- 卡片尺寸：320px × 280px（3列网格）
- 卡片间距：24px
- 图标区：64px × 64px，圆角 12px
- 评分星星：12px 黄色 `#fbbf24`
- 标签：4px 圆角，内边距 4px 8px
- 价格：品牌色数字

---

### 12.5 Consumer Dashboard（订阅者控制台）

**设计目标**：清晰的订阅管理和用量展示

```
┌────────────────────────────────────────────────────────────────────────────┐
│  我的订阅                                                                 │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌─ 概览卡片 ───────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │   │
│  │  │   ¥128   │  │    3     │  │   ¥320   │  │   85%    │      │   │
│  │  │ 账户余额  │  │ 订阅中   │  │ 本月消费  │  │ 配额使用  │      │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│  ┌─ 订阅列表 ───────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  ┌─────────────────────────────────────────────────────────────┐ │   │
│  │  │  🤖 品牌文案助手                          [已激活] 🟢      │ │   │
│  │  │  ────────────────────────────────────────────────────────    │ │   │
│  │  │  订阅时间: 2026-03-15    剩余配额: 45/100 次             │ │   │
│  │  │                                                             │ │   │
│  │  │  ████████████████░░░░░░░░░░░░░  45%                      │ │   │
│  │  │                                                             │ │   │
│  │  │  [使用服务]  [续费]  [取消订阅]                            │ │   │
│  │  └─────────────────────────────────────────────────────────────┘ │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

### 12.6 Proactive Rules（触发规则管理）

**设计目标**：清晰的规则配置和状态展示

```
┌────────────────────────────────────────────────────────────────────────────┐
│  触发规则                                                                 │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌─ 概览 ─────────────────────────────────────────────────────────┐   │
│  │  3 条活跃规则  |  今日触发 12 次  |  最近触发: 5 分钟前          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│  [+ 新建规则]                                                             │
│                                                                            │
│  ┌─ 规则列表 ───────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  ┌─────────────────────────────────────────────────────────────┐ │   │
│  │  │  ⏰ 任务逾期提醒                              [启用] 🟢    │ │   │
│  │  │  ────────────────────────────────────────────────────────    │ │   │
│  │  │  触发: 任务逾期 1 天后                                      │ │   │
│  │  │  动作: 发送通知给任务负责人                                  │ │   │
│  │  │  优先级: 高                                                │ │   │
│  │  │  今日触发: 3 次  |  累计触发: 156 次                        │ │   │
│  │  │  [编辑]  [禁用]  [删除]                                    │ │   │
│  │  └─────────────────────────────────────────────────────────────┘ │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 十三、响应式规范

### 13.1 断点定义

| 断点 | 范围 | 侧边栏 | 网格列数 | 页面边距 |
|------|------|--------|----------|----------|
| **xs** | < 640px | 隐藏(抽屉) | 1 列 | 16px |
| **sm** | 640-767px | 隐藏(抽屉) | 2 列 | 20px |
| **md** | 768-1023px | 折叠(64px) | 3 列 | 24px |
| **lg** | 1024-1279px | 展开(240px) | 4 列 | 32px |
| **xl** | 1280-1535px | 展开(240px) | 6 列 | 40px |
| **2xl** | ≥ 1536px | 展开(240px) | 8 列 | 48px |

### 13.2 关键页面响应式

| 页面 | xs (<640px) | sm-md (640-1023px) | lg+ (≥1024px) |
|------|-------------|-------------------|--------------|
| Dashboard | 卡片单列 | 2列网格 | 4列网格 |
| Tasks | 列表视图 | 列表/看板 | 看板 + 详情 |
| Wiki | 单列 | 侧边栏折叠 | 侧边栏展开 |
| Settings | 全屏 Tab | Drawer Tab | 分栏布局 |

### 13.3 移动端导航

```
┌─────────────────────┐
│ ☰  TeamClaw    🔍  │  ← 移动端 Header
└─────────────────────┘
│                     │
│   [Dashboard]       │  ← 抽屉式导航
│   [Tasks]           │
│   [Wiki]            │
│   [Skills]         │
│   ...               │
│                     │
│   ─────────         │
│   [Settings]        │
│   [Help]            │
└─────────────────────┘
```

---

## 十四、交互模式规范

### 14.1 状态反馈规范

| 状态 | 视觉表现 | 交互表现 |
|------|---------|---------|
| **默认** | 正常样式 | 可交互 |
| **悬停** | 背景变亮 + 轻微阴影 | 指针变为 pointer |
| **激活** | 品牌色边框/背景 | 立即响应 |
| **加载** | 骨架屏/微调样式 | 禁用交互 |
| **成功** | 绿色渐变 + 动画 | 自动消失或显示确认 |
| **错误** | 红色边框/背景 | 显示错误信息 |
| **禁用** | 透明度 50% | 禁止交互 |

### 14.2 错误处理层级

```
┌─────────────────────────────────────────────────────┐
│  错误处理层级                                       │
│                                                     │
│  Level 1: 内联错误（Inline）                       │
│  ├── 输入框下方红色提示文字                        │
│  └── 适用于：表单字段验证                          │
│                                                     │
│  Level 2: Toast 通知（Toast）                     │
│  ├── 右上角弹出，3-5秒自动消失                      │
│  └── 适用于：操作失败、成功、警告                  │
│                                                     │
│  Level 3: 确认对话框（Confirmation）               │
│  ├── 模态框，需要用户确认                          │
│  └── 适用于：危险操作确认                         │
│                                                     │
│  Level 4: 错误页面（Error Page）                  │
│  ├── 全屏错误提示，提供重试/返回选项                │
│  └── 适用于：严重错误、网络问题                    │
└─────────────────────────────────────────────────────┘
```

---

## 十五、图标规范

### 15.1 图标库

**主图标库**：Lucide React
- 风格：线性，2px 描边
- 尺寸：16px / 20px / 24px / 32px

### 15.2 自定义图标

| 图标 | 用途 | 颜色 |
|------|------|------|
| 🤖 AI 机器人 | AI 相关元素 | ai 色 |
| 👤 人物 | 人类用户 | human 色 |
| ⚡ 闪电 | 触发器/状态 | ai 色 |
| 🔀 分支 | 条件分支 | success 色 |
| 🔄 循环 | 循环节点 | pink 色 |
| ⏰ 定时 | 定时任务 | brand 色 |

---

## 十六、实施计划

### Phase 1: 基础重构（v1.1 早期）

#### Week 1-2: 布局系统
- [ ] 重构 AppShell2
- [ ] 重构 Sidebar2
- [ ] 重构 Header2
- [ ] 实现 CommandBar
- [ ] 实现响应式断点

#### Week 3-4: 设计系统
- [ ] 提取 Design Tokens
- [ ] 重构 Button/Card/Input v2
- [ ] 实现 DataTable v2
- [ ] 实现 Notification 系统
- [ ] 统一动画规范

### Phase 2: 页面重构（v1.1 中期）

#### Week 5-6: Dashboard + Settings
- [ ] 重构 Dashboard 2.0
- [ ] Settings 路由化拆分
- [ ] 统一状态展示

#### Week 7-8: 工作区页面
- [ ] Tasks 页面优化
- [ ] Wiki 页面优化
- [ ] Members 页面优化

### Phase 3: 新功能 UI（v1.1 后期）

#### Week 9-10: Workflow Editor
- [ ] WorkflowCanvas 基础
- [ ] 节点拖拽
- [ ] 属性面板
- [ ] 预览/运行

#### Week 11-12: 高级功能
- [ ] SkillGraph 可视化
- [ ] AuditLogViewer
- [ ] 移动端适配
- [ ] 性能优化

---

## 十七、交付物清单

### 设计师需要交付

| 序号 | 交付物 | 格式 | 说明 |
|------|--------|------|------|
| 1 | 设计源文件 | Figma / Sketch | 包含所有组件和页面 |
| 2 | 设计规范文档 | PDF / Notion | 颜色/字体/间距规范 |
| 3 | 组件规格表 | Figma / Excel | 所有组件的 props 说明 |
| 4 | 页面流程图 | Figma | 页面跳转关系 |
| 5 | 动效规范 | Figma / Lottie | 关键动效示例 |
| 6 | 响应式稿 | Figma | xs / sm / md / lg 四套 |
| 7 | 深色/浅色稿 | Figma | 两套配色方案 |

### 优先级排序

```
Phase 1（核心）:
├── 1. Dashboard 2.0 完整设计稿
├── 2. Settings 2.0 完整设计稿
├── 3. 基础组件库设计（Button/Input/Card/Badge/Table）
└── 4. 布局系统设计（AppShell/Sidebar/Header）

Phase 2（重要）:
├── 5. Workflow Editor 完整设计稿
├── 6. Marketplace 完整设计稿
└── 7. Consumer Dashboard 完整设计稿

Phase 3（完善）:
├── 8. Proactive Rules 完整设计稿
├── 9. 动效规范文档
└── 10. 响应式适配稿
```

---

## 十八、验收标准

### 18.1 功能验收

- [ ] 所有现有页面在 v2 布局下正常工作
- [ ] Settings 路由化拆分完成
- [ ] Dashboard 2.0 信息架构清晰
- [ ] Command Bar (⌘K) 功能完整
- [ ] 响应式布局在 3 个断点下正常

### 18.2 视觉验收

- [ ] Design Tokens 在所有组件中一致使用
- [ ] 动画流畅，无 jank
- [ ] Dark Mode 完整支持
- [ ] 组件变体统一
- [ ] 图标大小/风格统一

### 18.3 性能验收

- [ ] Lighthouse Performance ≥ 90
- [ ] First Contentful Paint < 1.5s
- [ ] Largest Contentful Paint < 2.5s
- [ ] 无 Layout Shift

---

## 十九、参考资料

| 文档 | 位置 |
|------|------|
| LLM 执行规划 | `docs/optimization/teamclaw_v1.1_llm_execution_plan.md` |
| UI 重构规划 | `docs/optimization/teamclaw_v1.1-ui-redesign.md` |
| 竞品参考：n8n | https://n8n.io |
| 竞品参考：Coze Studio | https://github.com/coze-dev/coze-studio |
| 组件库参考：shadcn/ui | https://ui.shadcn.com |

---

_本文档配合 LLM 执行规划使用_  
_Last updated: 2026-03-26_
