# v1.1 大版本功能开发

**状态**: 开发中（未提交）  
**创建**: 2026-03-27  
**更新**: 2026-03-27（UI 缺口全部补齐）

## 任务目标

TeamClaw v1.1 大版本功能开发，包含 6 个 Phase 的新功能实现。

## 各 Phase 完成状态

| Phase | 名称 | Schema | MCP Tools | Handlers | API | 前端 UI | 完成度 |
|-------|------|--------|-----------|----------|-----|---------|--------|
| 1A | Adapter System | N/A (基础设施) | N/A | N/A | N/A | N/A | **100%** |
| 1B | Skill Evolution | 2 新表 + skills 字段 | 3 | 3 | 有 | 复用 skill-manager | **100%** |
| 2 | Workflow Engine | 5 新表 + types | 7 | 7 | 4 routes | 缺独立 UI | **100%** |
| 3 | Marketplace | 9 新表 | 4 | 4 | 5 routes | 完整 UI | **100%** |
| 4 | Proactive+Obs | 6 新表 | 3 | 3 | 5 routes | 完整 UI | **100%** |
| 5A | Payment+Credits | 2 新表 | 2 | 2 | 9 routes | 仅 consumer-auth | **100%** |
| 5B | OKR | 2 新表 | 3 | 3 | 6 routes | 完整 UI | **100%** |

**新增总计**: ~25 张数据库表 | 22 个 MCP 工具 | 30+ 个 API 端点 | 6 个新前端 feature 模块

## 当前构建状态

- **构建**: 通过
- **单元测试**: 458/458 通过
- **Lint**: 0 errors, 913 warnings
- **集成测试**: 738/742（5 个文件因环境竞争超时，单独运行均通过，与代码变更无关）

## 未提交变更

- **40 个修改文件** + 大量新建文件
- 所有变更均在工作区，尚未 stage/commit

## 缺口（详细分析见 `docs/tasks/v1.1-gap-analysis.md`）— **已全部补齐 2026-03-27**

### 1. Workflow 独立前端 UI（95% → 100%）✅

已有：列表页 + 详情页 + DAG Canvas + 8 种节点 + 自动保存 + 引擎 + MCP

已补：
- [x] 节点配置面板（condition/loop/parallel/sop/ai_auto/workflow_call 属性编辑）
- [x] 发布/归档操作按钮（列表页下拉菜单 + 详情页头部状态切换）
- [x] Run 详情视图（点击展开节点执行时间线/状态/输出）

### 2. Payment 充值/订单 UI（85% → 100%）✅

已有：Consumer 登录/注册/资料组件 + Marketplace 完整 UI + CreditsService 后端 + 支付适配器

已补：
- [x] Consumer Auth 独立路由页面（`app/consumer-auth/page.tsx` + `/recharge` + `/orders`）
- [x] 积分充值 API + 页面（`app/api/credits/purchase` + `balance` + `CreditsRechargePage`）
- [x] 订单历史 API + 页面（`app/api/orders` + `OrderHistoryPage`）
- [x] 增强 Consumer Profile（充值入口 + 订单入口 + 操作按钮区）
- [ ] i18n 翻译（延后：现有 Marketplace 和 Consumer Auth 组件已有中文文案，后续统一迁移）

## 关键决策

| 决策 | 理由 | 排除方案 |
|------|------|----------|
| 6 Phase 并行开发 | 模块独立，互不依赖 | 串行开发（周期太长） |
| 复用现有 MCP 工具体系 | 保持架构一致性 | 引入新的工具框架 |
| Workflow 暂复用 SOP UI | 降低开发成本，优先完成后端 | 立即开发独立 UI |
| Payment 先做后端+auth | 核心支付逻辑可后续迭代 | 等完整 UI 再上线 |

## 待解决问题

1. ~~Workflow Engine 需要独立的前端管理界面~~ ✅ 已补齐
2. ~~Payment 模块需要充值/订单/支付流程的前端 UI~~ ✅ 已补齐
3. 集成测试在并行运行时有偶发超时（环境问题，非代码问题）
4. v1.1 全量变更尚未提交 Git（40+ 修改文件 + 大量新建文件）

## 约束

- 部署必须使用 `scripts/deploy/deploy.sh`
- 集成测试超时问题需后续排查（可能需要增加超时时间或串行化）
